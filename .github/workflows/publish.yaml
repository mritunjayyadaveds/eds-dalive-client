name: Publish

on:
  repository_dispatch:
    types:
      - resource-published

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Publish Triggered
        run: echo "EDS Publish workflow registered"
